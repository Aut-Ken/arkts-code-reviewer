function unicodeChanged(): string {
  return "你好🚀"; // a deliberately long explanatory comment remains one lexical comment token
}
function unicodeSupport(): string {
  return "生命周期清理✅ and a long string literal remain one code token";
}
